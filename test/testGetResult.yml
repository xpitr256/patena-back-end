config:
  target: https://patena-api.herokuapp.com
  payload:
    # path relativo a la localización del script de test
    path: "orderNumber.csv"
    fields:
      - "OrderNumber"
  phases:
    - duration: 20
      arrivalRate: 1
      name: get Length
  defaults:
    headers:
      x-api-key: "{{ $processEnvironment.VUE_APP_BACK_END_TOKEN }}"
scenarios:
  - flow:
      - get:
          # TODO get valid token
          url: "/results/{{ OrderNumber }}"
          Autorization: "Bearer " + validToken




